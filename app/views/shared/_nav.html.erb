<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <div class="container-fluid">
    <a class="navbar-brand" href="#">Private Events</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav">
        <li class="nav-item">
          <a class="nav-link active" aria-current="page" href="<%= events_path %>">Events</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="<%= new_event_path %>">Create</a>
        </li>
        <li class="nav-item">
          <% if signed_in? %>
            <%= link_to "Sign out", logout_path, :method => :delete, class: "nav-link" %>
          <% else %>
            <%= link_to "Sign up", new_user_path, :method => :get, class: "nav-link" %>
          <% end %>
        </li>
        <li class="nav-item">
          <% unless signed_in? %>
            <%= link_to "Sign in", login_path, :method => :get, class: "nav-link" %>
          <% end %>
        </li>
      </ul>
    </div>
  </div>
</nav>
